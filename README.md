### Use the following commands to clone and install

#### Clone
```git clone https://github.com/NicholaiStaalung/TestCase.git```

#### install dependencies
```pip3 install -r TestCase/requirements.txt```

